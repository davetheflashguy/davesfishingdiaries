Wireframe preview for the Catches UI

Files:
- `catches_wireframe.html` — standalone HTML wireframe showing layout and filters
- `styles.css` — stylesheet for the wireframe

Preview:
- Open `ui/wireframes/catches_wireframe.html` in your browser to see the wireframe.

Next steps you might want:
- Convert the layout into Angular components (`Filters`, `CatchCard`, `Map`, `ResultsList`)
- Hook filters up to API calls to `/catches` with query params for server-side filtering
- Replace map placeholder with a map component (Leaflet/Mapbox)
